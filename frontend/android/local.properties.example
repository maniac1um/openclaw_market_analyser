## Android SDK 路径（本机填写，勿提交）

复制为 `local.properties`（与 `local.properties.example` 同目录）：

```properties
sdk.dir=/home/你的用户名/Android/Sdk
```

Linux 默认 SDK 目录通常是 `~/Android/Sdk`（安装 Android Studio 后自动生成）。

也可设置环境变量（二选一即可）：

```bash
export ANDROID_HOME=$HOME/Android/Sdk
export PATH=$PATH:$ANDROID_HOME/platform-tools:$ANDROID_HOME/tools/bin
```

查找本机 SDK 路径：

```bash
ls ~/Android/Sdk
# 或
find ~ -maxdepth 4 -type d -name platform-tools 2>/dev/null
```

若 `~/Android/Sdk` 不存在，请先安装 [Android Studio](https://developer.android.com/studio)，首次启动完成 Setup Wizard，并在 **SDK Manager** 中安装：

- Android SDK Platform **36**（与工程 `compileSdk` 一致）
- Android SDK Build-Tools
- Android SDK Platform-Tools
